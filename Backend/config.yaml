# 服务器配置
server:
  port: 8080
  mode: debug
  host: 0.0.0.0

# 数据库配置
database:
  type: mysql
  host: localhost
  port: 3306
  username: root
  password: 123456
  dbname: ticketdb
  charset: utf8mb4

# Redis配置
redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

# JWT配置
jwt:
  secret: damai_ticket_system_secret_key_2024
  expire: 7200

# CORS配置
cors:
  allowed_origins:
    - http://localhost:3000
    - http://localhost

# 上传配置
upload:
  path: ./uploads
  max_size: 5242880

# 秒杀配置
seckill:
  order_expire_minutes: 30
  max_quantity_per_user: 5
